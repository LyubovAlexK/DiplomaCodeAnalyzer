<UserControl x:Class="SimbirSoftCodeAnalyzer.Views.Pages.Admin.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👥 Пользователи"
                   Style="{StaticResource H1Text}" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="🔄 Обновить"
                    Style="{StaticResource OutlineButton}"
                    Click="RefreshButton_Click"
                    Margin="0,0,10,0"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource DataGridCardBorder}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid x:Name="UsersGrid"
                          Style="{StaticResource CustomDataGridStyle}"
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          SelectionMode="Single"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>