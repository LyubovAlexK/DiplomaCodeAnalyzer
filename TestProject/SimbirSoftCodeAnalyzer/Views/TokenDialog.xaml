<Window x:Class="SimbirSoftCodeAnalyzer.Views.TokenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Токен" Width="450" Height="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="White" Icon="/Resources/Images/logo-simbirsoft.png"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Значение:" Style="{StaticResource H4Text}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="ValueBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Описание:" Style="{StaticResource H4Text}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="DescBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>

        <Button Grid.Row="4" Content="Сохранить" Style="{StaticResource PrimaryButton}"
                Click="SaveButton_Click" HorizontalAlignment="Right" Padding="20,8"
                FontSize="{DynamicResource AppFontSizeH4}"/>
    </Grid>
</Window>